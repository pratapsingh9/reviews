import { Router } from "express";
// import {z} from 'zod'
const router = Router();

router.get('/login',async(req,res)=>{
    try {
        const {email,body} = req.body;

    } catch (error) {
        
    }
})